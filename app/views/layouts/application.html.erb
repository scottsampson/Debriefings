<!DOCTYPE html>
<html>
<head>
  <title>Debriefings</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
<% if @show_navigation %>
  <ul id="navigation">
    <li><%= link_to 'Questions', questions_path, :class => ( "on" if current_tab?('participants') ) %></li>
    <li><%= link_to 'Projects', projects_path, :class => ( "on" if current_tab?('point_types') ) %></li>
    <li><%= link_to 'Answers', answers_path, :class => ( "on" if current_tab?('points') ) %></li>
  </ul>
<% end %>  

<%= yield %>

</body>
</html>
